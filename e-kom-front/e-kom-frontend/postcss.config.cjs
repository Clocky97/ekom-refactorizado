module.exports = {
  plugins: {
    // Tailwind no está en uso en este proyecto (estilos escritos manualmente).
    // Si prefieres usar Tailwind, instala "tailwindcss" y vuelve a activarlo.
    autoprefixer: {},
  },
};